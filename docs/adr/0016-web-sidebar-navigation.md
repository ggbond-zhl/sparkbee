# 前端应用壳层使用侧边导航

SparkBee 前端应用壳层使用桌面侧边导航替代顶部导航，移动端保留紧凑顶部栏并通过菜单按钮打开侧边导航。SparkBee 是充电桩调试台，后续会逐步增加桩实例、运行、交易、诊断等工作入口，侧边导航比顶部导航更适合承载工作型信息架构。

侧边栏优先使用 shadcn/ui 的 `Sidebar` 组件组合，只在 `app/ui` 下封装 SparkBee 自己的导航内容和布局；V1 不做桌面折叠，当前只展示已实现的“桩实例”入口。视觉和组件配置遵循 shadcn/ui 默认配置。
