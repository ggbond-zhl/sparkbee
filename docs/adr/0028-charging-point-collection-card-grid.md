# 充电桩实例集合使用响应式 Card 网格

充电桩实例集合以对象浏览和单项操作为主，不再套用 ADR-0017 的共享 `DataTable`，而是统一使用响应式 Card 网格：小于 `md` 时单列并无限滚动，`md` 及以上使用两至三列和分页。日志、交易等高密度、强比较列表仍使用 `DataTable`；这样既保留共享表格边界，也让充电桩的身份、CSMS、型号和操作在不同屏幕上保持可读。
