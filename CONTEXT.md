# SparkBee

SparkBee 是一个可自部署的充电桩模拟调试台，用于让少量虚拟充电桩长期连接到 CSMS，并人工完成 OCPP 充电调试流程。

## Language

**CSMS**:
充电桩通过 OCPP 连接的中心系统，负责接收桩上报并下发远程命令。
_Avoid_: 平台、中心端、后台平台

**桩实例**:
SparkBee 中一个长期存在、可启动或停止的虚拟充电桩，英文上下文统一称为 chargingPoint。它包含 OCPP 协议、CSMS 地址、桩身份和枪口拓扑。
_Avoid_: station、设备、模拟器、充电桩配置

**运行调试台**:
面向单个桩实例的操作与观察界面，用于执行运行控制、枪口动作、交易动作并查看实时运行反馈。
_Avoid_: 配置详情页、档案页、静态详情页

**桩身份**:
桩实例连接 CSMS 时使用的 charge point identity，并参与生成最终 WebSocket 地址。
_Avoid_: 设备编号、桩编号、连接路径

**桩名称**:
用户在 SparkBee 内部识别桩实例的展示名，不参与 OCPP 协议连接，也不替代桩身份。
_Avoid_: 桩身份、charge point identity、连接路径

**协议版本**:
桩实例模拟充电桩并连接 CSMS 时使用的 OCPP 版本。
_Avoid_: 协议类型、OCPP 类型、连接协议

**协议配置项**:
桩实例在指定 OCPP 协议版本下拥有的配置键及当前值，例如 HeartbeatInterval；它不包含桩名称、桩身份、CSMS 地址等桩实例基本配置。
_Avoid_: Configuration、充电桩配置、桩实例基本配置

**协议配置目录**:
某个桩实例在指定协议版本下拥有的全部内置协议配置项；不同协议版本的目录彼此隔离。
_Avoid_: 自定义配置集合、跨协议配置、配置覆盖项

**待重启配置项**:
已经接受并持久化新值，但需要桩实例成功重新启动后才应用该值的协议配置项。
_Avoid_: 保存失败配置、未提交配置、草稿配置

**枪口**:
用户在 V1 前端直接管理的充电连接点；OCPP 1.6 界面使用 connectorId 作为枪口编号且不展示 EVSE ID，编号必须大于 0，0 代表充电桩本身。V1 不要求用户理解 EVSE，内部可以映射到协议核心的 EVSE/connector 模型。
_Avoid_: connector、EVSE、插座

**枪口额定电压**:
用户为枪口配置的额定工作电压，用于 SparkBee 生成电压采样值并派生模拟功率。
_Avoid_: 最大电压、maxVoltage

**枪口额定电流**:
用户为枪口配置的额定工作电流，用于 SparkBee 生成电流采样值并派生模拟功率。
_Avoid_: 最大电流、maxCurrent

**模拟功率**:
SparkBee 为简化电量上报而从枪口额定电压和枪口额定电流派生的功率值；V1 不表达单相或三相差异。
_Avoid_: maxPower、真实输出功率、额定最大功率

**插枪**:
用户在运行中的桩实例上触发的车辆接入枪口模拟动作；它不包含鉴权或开始交易。
_Avoid_: 开始交易、启动充电、连接枪口

**拔枪**:
用户在运行中的桩实例上触发的车辆离开枪口模拟动作；存在活动交易时，拔枪会以车辆断开原因为该交易收尾。
_Avoid_: 手动停止交易、仅断开枪口、不结束交易

**插枪状态**:
某个枪口当前是否已经模拟插枪，用于判断能否继续鉴权、开始交易或拔枪。
_Avoid_: 车辆接入、车辆状态

**枪口状态摘要**:
运行调试台中按单个枪口汇总的当前运行信息，包括枪口状态、插枪状态、交易状态和最近表值；它描述当前状态，不等同于历史曲线。
_Avoid_: 运行摘要、图表区、历史统计

**鉴权**:
运行中的桩实例使用 idTag 向 CSMS 发起的校验动作；V1 前端不把它作为独立控制入口，而是在开始交易时一并触发。
_Avoid_: 独立鉴权按钮、刷卡启动、授权交易

**开始交易**:
用户在运行中的桩实例上对指定枪口启动本地交易的动作；成功表示本地交易已经活动，对应的 OCPP StartTransaction 可能仍在等待 CSMS 确认。
_Avoid_: 鉴权、插枪、启动桩实例、CSMS 已确认交易

**停止交易**:
用户在运行中的桩实例上对指定枪口显式结束本地交易的动作；成功表示本地交易已经结束，对应的 OCPP StopTransaction 可能仍在等待 CSMS 确认。拔枪也可以为活动交易收尾，但仍是不同的用户动作。
_Avoid_: 拔枪、停止桩实例、所有交易收尾、CSMS 已确认结束

**充电采样**:
交易过程中围绕某个枪口采集的一次计量点，包含采样时间、累计电量以及功率、电流、电压等电气量，用于形成运行调试台的充电曲线。
_Avoid_: 协议报文、日志、状态摘要

**充电曲线**:
运行调试台按枪口展示当前活动交易的充电采样序列；没有活动交易时不展示，也不拼接或回退到历史交易。
_Avoid_: 历史曲线、跨交易曲线、最近交易曲线

**运行状态**:
当前进程中桩实例实际所处的生命周期状态，例如 starting、running 或 stopped。starting 覆盖用户已发起启动但尚未进入 running 的全过程，包括连接中和首次 BootNotification 处理中。
_Avoid_: 连接状态、实时状态

**会话状态**:
桩实例与 CSMS 的当前通信会话状态，例如 online、reconnecting 或 offline；它描述 OCPP WebSocket 通信是否连通，不等同于桩实例 Actor 是否运行。
_Avoid_: 运行状态、桩状态、在线状态

**最后心跳时间**:
桩实例最近一次 Heartbeat 与 CSMS 完成交互的时间，用于判断通信活性；它不是界面数据刷新时间。
_Avoid_: 更新时间、状态更新时间、最后刷新时间

**可运行性**:
桩实例是否具备启动运行所需的静态条件，例如至少拥有一个枪口；它不表示当前进程中已经运行。
_Avoid_: 运行状态、连接状态

**调试就绪程度**:
运行中的桩实例当前是否适合继续执行人工调试动作，由 Boot 结果、枪口状态、车辆接入和交易状态等运行反馈共同决定。
_Avoid_: 运行状态、可运行性

**协议事件**:
协议核心产生的结构化状态变化，例如桩状态、会话状态、授权状态、交易状态和表值事件。
_Avoid_: 日志、回调、消息

**桩事件流**:
某个桩实例对外提供的实时协议事件序列，用于让前端按桩实例观察状态变化和协议交互；即使桩实例当前 stopped，订阅也代表观察它后续的运行变化，删除桩实例会终止这个事件流。
_Avoid_: 全局事件流、SSE 分组、事件频道

**运行状态快照**:
某个桩实例在当前服务进程中的可恢复运行态视图，用于让运行调试台在初次打开或刷新后恢复当前状态；它不是历史事件列表。
_Avoid_: 事件流、事件回放、页面缓存

**协议报文**:
桩实例与 CSMS 之间收发的 OCPP JSON 消息帧记录。
_Avoid_: 消息、日志、payload

**滚动钉住**:
运行调试台中用户让某个观察列表保持当前阅读位置的状态；新进入的协议报文或协议事件仍会被接收，但不会主动把视口推回最新位置。它按观察列表独立生效。
_Avoid_: 暂停、固定滚动、锁定滚动

**观察筛选**:
运行调试台中用户对保留期内历史观察记录进行的查询条件，并同样约束实时进入当前视图的协议报文或协议事件；筛选不删除底层记录。
_Avoid_: 仅过滤当前页面、删除筛选结果、本地临时筛选

**历史观察分页**:
运行调试台为协议报文和协议事件分别维护的可见历史范围；用户可以主动加载更早记录来扩展当前页签的范围，而该范围不等同于历史观察记录的保留上限。
_Avoid_: 历史记录保留上限、报文事件共享额度、无限实时列表

**历史观察记录**:
持久化保存、用于跨页面或服务进程回看调试过程的真实协议报文和协议事件；它不包含运行状态快照、桩实例删除通知或 Actor 日志，不参与恢复运行状态，也不作为业务判断的事实来源。
_Avoid_: 事件溯源、运行状态来源、业务审计日志

**Actor**:
SparkBee 中承载单个桩实例运行行为的执行主体，当前特指 ChargingPointActor，并与桩实例一一对应。
_Avoid_: 通用 Actor、后台任务、服务进程

**Actor 日志**:
SparkBee 为开发和测试排查 Actor 内部过程而保留的结构化诊断记录，关注内部行为、错误和关键决策点，不作为业务状态来源。
_Avoid_: 运行日志、协议事件、协议报文

**测试环境保活探测**:
外部调度器定期访问测试环境后端进程存活接口，以降低 Render 免费实例因空闲而休眠的概率；它是 best effort（尽力而为）机制，不构成持续可用性保证。
_Avoid_: 保证不休眠、永不休眠保障、定时就绪检查

**交易**:
一次从开始充电到结束充电的 OCPP transaction。SparkBee 记录交易用于调试和回看，不把它称为 session。
_Avoid_: 会话、充电会话

**活动交易**:
尚未结束并可继续产生充电采样的交易；它跨页面和服务进程持久存在，并要求桩实例在服务重启后自动恢复运行。
_Avoid_: 最近交易、页面交易、交易缓存

**离网交易**:
桩实例无法与 CSMS 通信时，依据本地授权信息或已启用的未知标识策略开始、继续或结束的交易；恢复通信后，它仍须接受 CSMS 的交易确认和授权结果。
_Avoid_: 离网充电、无网直充、强制离线启动、离线会话

**本地授权信息**:
桩实例在无法向 CSMS 请求鉴权时用于判断 idTag 的非易失授权依据，包括由 CSMS 维护的 Local Authorization List 和根据协议响应更新的 Authorization Cache；同一标识存在于两者时，前者优先。
_Avoid_: 本地白名单、临时缓存、UI 授权列表

**交易交付**:
交易相关协议动作可靠到达 CSMS 的过程，包括在线送达、离线暂存和恢复连接后的补送。未被 CSMS 确认的交付不会因通信中断或 SparkBee 运行进程更替而消失；它不等同于交易本身。
_Avoid_: 交易同步、报文发送、现场恢复

**交易交付记录**:
一条等待 CSMS 确认的交易消息事实，类型为交易开始、一次充电采样或交易结束；它表达消息的交易归属，但不决定多笔交易之间的发送顺序，也不是原始 OCPP 报文的副本。
_Avoid_: 离线报文队列、原始报文 outbox、补发任务

**桩级交易交付队列**:
同一桩实例所有待交付交易消息按实际生成先后形成的唯一顺序；存在积压时，后续产生的交易消息继续排在队尾，不越过较早消息，事件时间戳变化也不重新排列已经形成的顺序。
_Avoid_: 单交易补发队列、并行补发、按交易整笔补发

**待交付交易消息**:
已经发生但尚未获得 CSMS 成功确认、也尚未进入交付失败终态的交易消息；它不因历史观察记录的保留期限届满而自动消失。
_Avoid_: 历史协议报文、临时报文、过期补发数据

**交易交付积压**:
桩实例仍有较早的待交付交易消息尚未离开活动队列的状态；即使通信已经恢复，新的交易动作仍可在本地发生，但对应交易消息必须追加到队尾。
_Avoid_: 离线状态、连接失败、阻止新交易

**至少一次交易交付**:
待交付交易消息在未收到 CSMS 成功确认时允许使用同一消息标识再次发送，以优先避免遗漏；当响应在通信中丢失时，CSMS 仍可能重复处理该消息。
_Avoid_: 恰好一次交付、保证去重、至多一次交付

**交付失败**:
待交付交易消息达到有限发送次数后仍未得到 CSMS 成功确认的终态；它退出活动交付队列并保留失败事实，使后续交易消息可以继续交付。
_Avoid_: 静默丢弃、永久重试、队列阻塞

**交付尝试**:
待交付交易消息在持久记录中进入发送中状态并交给通信会话的一次发送机会；尚未在线的等待不构成交付尝试，发送后结果未知仍计为一次。
_Avoid_: 重连次数、轮询次数、页面重试
