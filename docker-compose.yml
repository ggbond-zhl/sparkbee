services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: sparkbee
      POSTGRES_USER: sparkbee
      POSTGRES_PASSWORD: sparkbee
    ports:
      - "5432:5432"
    volumes:
      - sparkbee-postgres:/var/lib/postgresql/data

volumes:
  sparkbee-postgres:
