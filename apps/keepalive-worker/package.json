{
  "name": "@spark-bee/keepalive-worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:unit": "vitest run tests/unit",
    "typecheck": "tsc --noEmit",
    "types": "wrangler types --include-runtime false --strict-vars false"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vitest": "^4.0.15",
    "wrangler": "^4.110.0"
  }
}
